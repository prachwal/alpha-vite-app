{
    "buildCommand": "npm run build",
    "outputDirectory": "./",
    "functions": {
        "api/index.js": {
            "maxDuration": 30
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/api"
        }
    ]
}